<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Symbol</th>
      <th>Current price</th>
      <% if @user.id == current_user.id %>
        <th>Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @user_stocks.each do |user_stock| %>
      <tr>
        <td><%= user_stock.name %></td>
        <td><%= user_stock.ticker %></td>
        <td><%= user_stock.price %></td>
        <% if current_user.id == @user.id %>
          <td><%= link_to 'Delete', user_stock_path(user_stock), :method => :delete, :data => {confirm: "Are you sure?"}, class: "btn btn-xs btn-danger" %></td>
          <%# i am pretty sure this is wrong. user_stock is actually a stock object, so when u pass in a stock object, it is using stock object id, not the user_stock id %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>